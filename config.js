module.exports = {
		host: 'localhost',
		port: 3306,
		user: 'fred', password: '181204Qw!',
		database: 'northwind',                                          //change
		connectionLimit: 4,
		sessionSecret: 'my secret'
}

// rmb to create in local instead, then grant access to fred